// Declarando uma variável contendo uma String
var nome = 'Letícia';
console.log(typeof nome, nome);

// Declarando uma variável contendo um número dentro de uma String
var idade = 21;
var frase = `Eu tenho ${idade} anos de idade`;
console.log(typeof frase, frase);

// Declarando 2 variáveis, uma com nome e outra com sobrenome, depois somo elas
var meuNome = 'Letícia';
var meuSobrenome = 'Sena';
var nomeCompleto = `${meuNome} ${meuSobrenome}`;
console.log(nomeCompleto);

// Frase "It's time" em uma variável
var fraseDois = 'It\'s time';
console.log(fraseDois);

// Verificando que tipo de variável tem a var nome
console.log(typeof nome);