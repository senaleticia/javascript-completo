const div = document.querySelector('div');
console.log(Object.prototype.toString.call(div));

div.dataset.height = 1000;
console.log(div.dataset);

// Adicionando um atributo "data-anime=show-down" e "data-anime=show-right" em todas as section com descrição dos animais

// Adicionando a classe "show-down" ou "show-right" a sua respectiva section, assim que a mesma aparecer na tela (animação tab)

// Fazendo com que o "show-down" anime de cima para baixo e "show-right" continue com a mesma animação da esquerda para a direita, no CSS

// Substituindo todas as classes js- por data atributes